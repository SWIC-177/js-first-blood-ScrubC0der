// Define a function that greets the name
function greet(name) {
    return "Hello," + name;
}

// Call the function 
var name = "your name";
var greeting = greet(yourName);

// Log console
console.log(greeting)